"use strict";


/**
 * Calculate the information gain at a specified branch node.
 * what do you need to take in the info gain?
 *
 *
 *
 * trainingData <ar
 *
 *
 */
/**
 *
 * @param trainingData <array> - an array of objects, each one is a data input having all of the attributes expressed
 *      in the attributeMap
 * @param attributeMap <object> - a map describing each of the attributes, their possible values, and a test function
 *      for that attribute to be applied to data inputs
 */
const ig = (trainingData, attributeMap) => {
    attributeMap.forEach
};
