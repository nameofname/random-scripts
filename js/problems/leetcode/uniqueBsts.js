function numTrees(n) {
    let total = 0;
    _makeTrees(set, remainder) {
        // do it make them!!!!
        if (set.length === 0) {
            set = 1;
            --remainder
            return _makeTrees(set, remainder);
        }
    }
    _maekTrees([], n);
    return total;
};